# Packages to use in making of cli

### cli styling and TUI

- `ink` : for TUIs and rounded boxed outputs
- `cli-spinners` : for good looking spinners
- `@clack/prompts` : for minimal TUIs
- `clipboardy` : for clipboard operations
- `cfonts` : for goog intro
